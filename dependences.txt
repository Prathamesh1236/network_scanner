sudo apt update
sudo apt install python3 python3-pip python3-venv nmap -y


mkdir flask_nmap
cd flask_nmap
python3 -m venv venv
source venv/bin/activate
pip install flask python-nmap

nano app.py

mkdir templates
nano templates/index.html

cd ~/nmap_project
python3 -m venv venv
source venv/bin/activate
pip install flask python-nmap
python3 app.py
deactivate


note -> source venv/bin/activate  activate env before running app








FROM python:3.9-slim

WORKDIR /app

# Copy the virtual environment (only if necessary)
COPY venv /app/venv

# Ensure the virtual environment is activated when running
ENV VIRTUAL_ENV=/app/venv
ENV PATH="$VIRTUAL_ENV/bin:$PATH"

# Copy the rest of the application
COPY . .

EXPOSE 5000

# Run the Flask app using the virtual environment
CMD ["flask", "run", "--host=0.0.0.0", "--port=5000"]
















